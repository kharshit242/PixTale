{
  "name": "backend",
  "version": "1.0.0",
  "main": "app.py",
  "scripts": {
    "start": "uvicorn app:app --host 0.0.0.0 --port 3000",
    "dev": "uvicorn app:app --reload"
  },
  "dependencies": {},
  "devDependencies": {},
  "description": "Backend for PixTale using FastAPI"
}
